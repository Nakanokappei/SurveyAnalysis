# ---- .NET build artifacts ----
bin/
obj/
[Pp]ublish/
publish-osx/
publish-win/
*.user
*.suo
project.lock.json

# ---- Rider / VS / VS Code ----
.idea/
.vs/
.vscode/

# ---- macOS / AppleDouble ----
# This volume is shared with Parallels/Windows; ._ resource-fork files appear and
# break code signatures. Never track them.
.DS_Store
._*

# ---- Windows ----
# Explorer thumbnail cache / folder artefacts on the Parallels-shared volume.
Thumbs.db
ehthumbs.db
Desktop.ini

# ---- Claude local settings (machine-local) ----
.claude/settings.local.json

# ---- Internal / not for the public repository ----
# Design & handoff notes and the sample survey data (the images/CSV contain
# realistic personal information) are kept locally but excluded from publishing.
/data/
/CLAUDE.md
/HANDOFF.md
/HANDOFF-Windows.md
